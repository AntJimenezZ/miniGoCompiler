package main;

var culito = 3;

func ComputeFac(num int) int {
	var numaux = 2;
	var caracter = 3; //validación de que se permita declarar un identificador con el mismo nombre de uno global     NO SIRVE
	var culito = 4;
	return 0; //validar el error de tipos con el retorno
};

var ano = 2;